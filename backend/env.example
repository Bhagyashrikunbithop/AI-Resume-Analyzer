# Comma-separated list of frontend origins allowed to call this API.
# Use "*" only for local development.
ALLOWED_ORIGINS=https://your-app.lovable.app,http://localhost:8080

# Port the service listens on (most hosts inject this automatically).
PORT=8000
